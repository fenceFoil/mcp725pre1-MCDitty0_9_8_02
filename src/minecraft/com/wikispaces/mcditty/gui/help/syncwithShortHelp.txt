SyncWith [voice] [optional layer]

Advances (or retreats!) the time of the current voice and layer to match the time of [voice] and [layer]. 

If [layer] isn't given, SyncWith syncs with the longest layer in [voice] automatically.

This is like a more-specific SyncVoices.

§bSee:§r Reset, SyncVoices